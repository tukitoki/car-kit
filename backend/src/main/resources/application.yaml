spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/car_kit
    username: postgres
    password: RasP91AS
  jpa:
    hibernate:
      ddl-auto: update
